spring:
  application:
    name: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/flowbank
    username: flowbank
    password: flowbank
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration