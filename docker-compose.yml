version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: flowbank-postgres
    environment:
      POSTGRES_DB: flowbank
      POSTGRES_USER: flowbank
      POSTGRES_PASSWORD: flowbank
    ports:
      - "5432:5432"